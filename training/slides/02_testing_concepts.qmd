---
title: "Concepts de testing"
subtitle: "Formation testing — Module 02"
format:
  revealjs:
    theme: simple
    slide-number: true
    code-block-height: 400px
---

## Anatomie d'un test

```
GIVEN  un fichier TSV avec 20 lignes             ← Arrange
WHEN   je lance validate_records.sh              ← Act
THEN   le code de sortie est 0                   ← Assert
AND    la sortie indique "20 records validés"
```

GIVEN / WHEN / THEN = Arrange / Act / Assert — même concept, deux notations.

---

## Quatre types de tests

| Type | Périmètre | Vitesse |
|------|-----------|---------|
| **Unitaire** | une fonction, un process | rapide |
| **Intégration** | plusieurs modules enchaînés | moyen |
| **Fonctionnel** | pipeline complet, end-to-end | lent |
| **Non-régression** | comparaison avec une sortie attendue | variable |

---

## Happy path vs edge case

Le même pattern — le GIVEN change, pas la structure.

```bash
# GIVEN records.tsv (20 lignes valides)   ← happy path
run bash validate_records.sh records.tsv  # WHEN
[ "$status" -eq 0 ]                       # THEN

# GIVEN empty.tsv (en-tête seul)          ← edge case
run bash validate_records.sh empty.tsv    # WHEN
[ "$status" -eq 1 ]                       # THEN

# GIVEN fichier absent                    ← edge case
run bash validate_records.sh missing.tsv  # WHEN
[ "$status" -eq 1 ]                       # THEN
```

---

## Le bug que Python ne signale pas

```python
def filter_by_threshold(df, threshold=45):
    return df[df["value"] > threshold]   # > au lieu de >=
```

. . .

Le programme ne plante pas. S02 (value=45) est silencieusement exclu.

```python
# Test qui passe quand même
result = filter_by_threshold(df, threshold=45)
assert len(result) < len(df)   # True — mais combien de lignes manquent ?
```

::: {.callout-tip}
Un bug silencieux ne lève pas d'exception — il produit un résultat légèrement faux.
:::

---

## Tests rapides vs tests lents

| Rapides (< 1 min) | Lents (> 1 min) |
|-------------------|-----------------|
| Fonctions unitaires | Pipeline complet |
| Process Nextflow | Tests containers |
| Snapshots existants | Golden dataset |

→ Les rapides passent à chaque PR.
→ Les lents passent en CI nightly ou manuellement.

---

## Assertions

Une assertion = une vérification binaire.

```python
# Python / pytest
assert output.exists()
assert line_count(output) == 100
assert output.suffix == ".html"
```

```bash
# Bash / bats
[ -f "$output" ]
[ "$(wc -l < "$output")" -eq 100 ]
```

---

## Les assertions utiles

| Ce qu'on vérifie | Exemple |
|-----------------|---------|
| Fichier existe | `assert path.exists()` |
| Nombre de lignes | `assert len(df) == 20` |
| Colonnes attendues | `assert "id" in df.columns` |
| Valeur dans une borne | `assert (df["value"] >= 0).all()` |
| Code de sortie | `assert returncode == 0` |

---

## Snapshots

Un snapshot = **photographie** de la sortie attendue.

```groovy
// nf-test — première exécution : crée le snapshot
assert snapshot(output).match()
// exécutions suivantes : compare — différence → échec
```

Scénario type : refactoring → `nf-test test` → diff détecté → bug identifié avant merge.

::: {.callout-warning}
Un snapshot ne dit pas que la sortie est **correcte**. Il dit qu'elle n'a **pas changé**.

Snapshot seul = filet de non-régression. Snapshot + assertion explicite = test utile.
:::

---

## Données de test

**Bonne pratique :** les données de test doivent être **minimalistes**.

```
data/
├── records.tsv    # 20 lignes (id, group, value)
└── empty.tsv      # en-tête seul, aucune donnée
```

Pourquoi petit ? Tests rapides, CI rapide, pas de dépendance aux gros fichiers.

---

## CI/CD

```
Push → GitHub Actions → Tests → OK ou ÉCHEC
```

| Déclencheur | Tests lancés |
|-------------|-------------|
| Chaque PR | pytest, bats, testthat, nf-test |
| Manuel | tests containers (Apptainer) |

Si ÉCHEC : merge bloqué automatiquement.

---

## Couverture : 5 tests, 100 % — et 3 bugs

```bash
$ pytest --cov=filter_and_summarise test_coverage_demo.py

Name                         Stmts   Miss  Cover
-------------------------------------------------
filter_and_summarise.py         14      0   100%

5 passed in 0.4s
```

::: {.callout-warning}
La couverture mesure si les lignes sont **exécutées**.

Elle ne mesure pas si vos assertions **prouvent quelque chose**.
:::

---

## Ce que la couverture ne voit pas

```python
# Bug 1 — > au lieu de >= : S02 (value=45) silencieusement exclu
assert len(filtered) == 10              # ← compte exact
assert 45 in filtered["value"].values  # ← invariant de borne

# Bug 2 — stats calculées sur df, pas sur filtered
assert result["A"] == pytest.approx(65.25)  # ← valeur métier

# Bug 3 — retourne Series, pas DataFrame
assert isinstance(result, pd.DataFrame)     # ← type explicite
```

→ La couverture dit ce qu'on n'a **pas testé**. Pas si ce qu'on a testé est **juste**.

---

## Suite → Module 03

Ces concepts s'appliquent aux exercices 01, 02 et 03.

Le module suivant montre comment les appliquer concrètement en Bash, Python et R.
